.logos.portlet
  .portlet-header
    Logos
  .portlet-content
    = image_tag(@location.logo.image.url, :height => "100")
    = form_for :logo, :html => {:multipart => true}, :url => {:action => "create", :controller => "logos", :location_id => location.id} do |f|
      = file_field :logo, "image"
      = f.submit

